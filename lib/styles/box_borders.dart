part of styles;
class BoxBorders{
  static const primaryBoxBorder=BorderRadius.all(
              Radius.circular(6));
               static const secondaryBoxBorder=BorderRadius.only(
                 topLeft:  Radius.circular(10),topRight:  Radius.circular(10)
             );
}

class ButtonBorders{
   static const primaryBoxBorder=BorderRadius.all(
              Radius.circular(10));
}